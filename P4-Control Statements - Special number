#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    int m,n,i,a,b,c,d,e;
    scanf("%d", &m);
    scanf("%d", &n);
    for(i=m;i<=n;i++)
    {
        a=i%10;
        b=i/10;
        c=a+b;
        d=a*b;
        e=c+d;
        if(e==i) 
            printf("%d\n", e);
    }
    return 0;
}
