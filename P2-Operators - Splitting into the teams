#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    int a, b, c, d;
    scanf("%d", &a);
    scanf("%d", &b);
    c = a / b;
    d = a % b ;
    printf("The number of friends in each team is %d and left out is %d", (int)c, d );
    return 0;
}
